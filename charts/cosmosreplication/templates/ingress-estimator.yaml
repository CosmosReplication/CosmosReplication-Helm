{{- if and .Values.estimator.enabled .Values.estimator.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "cosmosreplication.fullname" . }}-estimator
  labels:
    {{- include "cosmosreplication-estimator.labels" . | nindent 4 }}
  {{- with .Values.estimator.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.estimator.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.estimator.ingress.tls }}
  tls:
    {{- range .Values.estimator.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.estimator.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "cosmosreplication.fullname" $ }}-estimator
                port:
                  number: {{ $.Values.estimator.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
